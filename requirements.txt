# Schema Inference Tool — additional dependencies
# Install after main requirements.txt:
#   pip install -r requirements-schema-inference.txt

# Fuzzy string matching (faster than fuzzywuzzy, better recall on abbreviations)
rapidfuzz>=3.6.0

# Pydantic v2 — typed models for profiles, proposals, definitions
pydantic>=2.5.0,<3.0.0

# Rich terminal output — tables, panels, prompts for review CLI
rich>=13.7.0

# Anthropic SDK — LLM pass (Claude Haiku) for ambiguous columns
anthropic>=0.34.0
